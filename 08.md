# 08

在老家第一天，完全不想刷题，继续力扣的入门题单。

## 1512. 好数对的数目

```py
class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        ans = 0
        cnt = defaultdict(int)
        for x in nums:
            ans += cnt[x] # 先计算之前出现的次数
            cnt[x] += 1
        return ans
```
